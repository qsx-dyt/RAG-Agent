server {
  listen 80;
  # 允许上传较大文档(对齐后端的 20MB 限制,避免 nginx 默认 1MB 的 413)
  client_max_body_size 20m;
  location /api/ { proxy_pass http://api:8000; }
  location / { root /usr/share/nginx/html; try_files $uri /index.html; }
}
